const SECOND_IN_MS = 1000
const MAX_SECONDS = import.meta.env.VITE_MAX_SECONDS
const MAX_SECONDS_IN_MS = MAX_SECONDS * SECOND_IN_MS
const ALERT_CLOCK_SECONDS = 10

export {
  SECOND_IN_MS,
  MAX_SECONDS,
  MAX_SECONDS_IN_MS,
  ALERT_CLOCK_SECONDS,
}
